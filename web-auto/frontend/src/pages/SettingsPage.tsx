import { Box, Typography } from '@mui/material';

export function SettingsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h6">SettingsPage (migration in progress)</Typography>
    </Box>
  );
}
