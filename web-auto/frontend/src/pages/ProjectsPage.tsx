import { Box, Typography } from '@mui/material';

export function ProjectsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h6">ProjectsPage (migration in progress)</Typography>
    </Box>
  );
}
