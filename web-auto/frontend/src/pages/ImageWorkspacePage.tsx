import { Box, Typography } from '@mui/material';

export function ImageWorkspacePage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h6">ImageWorkspacePage (migration in progress)</Typography>
    </Box>
  );
}
